diff a/source/blender/draw/engines/npr/shaders/npr_strokegen_contour_processing.glsl b/source/blender/draw/engines/npr/shaders/npr_strokegen_contour_processing.glsl	(rejected hunks)
@@ -278,9 +278,6 @@ void main()
 
 
 
-
-
-#if defined(_KERNEL_MULTICOMPILE__CONTOUR_EDGES_2D_RESAMPLE)
 vec3 hash32(vec2 p) 
 {
 	vec3 p3 = fract(vec3(p.xyx) * vec3(.1031, .1030, .0973));
@@ -299,10 +296,13 @@ vec3 rand_col_rgb(uint seed0, uint seed1)
 
     float hue = rnd.x;
     float saturation = 0.6 + rnd.z*0.4;
-    float luminosity  = 0.6 + rnd.y*0.4;
+    float luminosity  = 0.6 + rnd.y*0.2;
 
     return hsl2rgb(vec3(hue, saturation, luminosity)); 
 }
+
+
+#if defined(_KERNEL_MULTICOMPILE__CONTOUR_EDGES_2D_RESAMPLE)
 /*
 	ssbo_bnpr_mesh_pool_counters_
 	ssbo_tree_scan_infos_2d_resampler_
@@ -990,15 +990,18 @@ void main()
 	float stylized_width_arc_len = 1.0f - abs(arc_len_param - .5f) * 2.0f; 
 	float stylized_width_stk_len = 1.0f; 
 	if (seg_len < 24u) stylized_width_stk_len = float(seg_len) / 24.0f; 
+	float stk_width = pcs_stroke_width_ * stylized_width_arc_len * stylized_width_stk_len; 
+	// stk_width = 3.0f; 
 	mat3x2 verts = compute_wing_quad_verts(
 		pos, pcs_screen_size_.xy, 
-		vec2(tangent.y, -tangent.x), pcs_stroke_width_ * stylized_width_arc_len * stylized_width_stk_len
+		vec2(tangent.y, -tangent.x), stk_width
 	);
 	for (uint i = 0; i < 3; i++)
 		verts[i] /= vec2(pcs_screen_size_.xy); // back to 01-uv space
 
 
 	vec4 col = vec4(.0f, .0f, .0f, 1.0f); 
+	// col.rgb = rand_col_rgb(seg_len, seg_len); 
 	if (cf.occluded_filtered) col.a = .0f; 
 	
 	if (valid_thread) 
